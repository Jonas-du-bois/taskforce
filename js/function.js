// Ajouter un événement lorsque la page est entièrement chargée
//window.addEventListener('load', function() {
    // Ajouter la classe "loaded" au body une fois la page complètement chargée
//    document.body.classList.add('loaded');
//});